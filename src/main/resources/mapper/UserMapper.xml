<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.runnersoftware.auto_test.mapper.UserMapper">

    <resultMap type="com.runnersoftware.auto_test.model.User" id="UserMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="password" column="password" jdbcType="VARCHAR"/>
        <result property="createTime" column="create_time" jdbcType="TIMESTAMP"/>
        <result property="username" column="username" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="UserMap">
        select id,
               password,
               create_time,
               username
        from bugs.user
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="UserMap">
        select id,
               password,
               create_time,
               username
        from bugs.user
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="UserMap">
        select
        id, password, create_time, username
        from bugs.user
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="password != null and password != ''">
                and password = #{password}
            </if>
            <if test="createTime != null">
                and create_time = #{createTime}
            </if>
            <if test="username != null and username != ''">
                and username = #{username}
            </if>
        </where>
    </select>
    <select id="findByUsername" resultType="com.runnersoftware.auto_test.model.User"
            parameterType="java.lang.String">
        select id,
               password,
               create_time,
               username
        from bugs.user where username = #{username}
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into bugs.user(password, create_time, username)
        values (#{password}, #{createTime}, #{username})
    </insert>

    <insert id="insertBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bugs.user(password, create_time, username)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.password}, #{entity.createTime}, #{entity.username})
        </foreach>
    </insert>

    <insert id="insertOrUpdateBatch" keyProperty="id" useGeneratedKeys="true">
        insert into bugs.user(password, create_time, username)
        values
        <foreach collection="entities" item="entity" separator=",">
            (#{entity.password}, #{entity.createTime}, #{entity.username})
        </foreach>
        on duplicate key update
        password = values(password) , create_time = values(create_time) , username = values(username)
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update bugs.user
        <set>
            <if test="password != null and password != ''">
                password = #{password},
            </if>
            <if test="createTime != null">
                create_time = #{createTime},
            </if>
            <if test="username != null and username != ''">
                username = #{username},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete
        from bugs.user
        where id = #{id}
    </delete>

</mapper>

